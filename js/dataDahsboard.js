/*var dashboardData = [];

function lançamentos() {  dashboardData = JSON.parse(this.responseText); }

function error(err) { alert('Erro:', err); }

var xhr = new XMLHttpRequest();
xhr.onload = lançamentos;
xhr.onerror = error;
xhr.open('GET', "/src/lancamentos.json", false);
xhr.send();

dataAtual = new Date().toDateString();

function InserindoVencimentos (cod, dataVencimento, descriçao, valor){
    var tb = document.getElementById('tbVencimentos');
    var qtdLinhas = tb.rows.length;
    var linha = tb.insertRow(qtdLinhas);

    var cellCodigo = linha.insertCell();
    var cellVencimento = linha.insertCell();
    var cellDescriçao = linha.insertCell();
    var cellValor = linha.insertCell();
}

function PreenchendoTBVencimentos() {
    for (var i = 0; i < this.dashboardData.length; i++){
        if ( this.dashboardData[i].Data = "30/08/2022") {
            InserindoVencimentos(this.dashboardData[i].Cod, this.dashboardData[i].Data, this.dashboardData[i].Descriçao, this.dashboardData[i].Valor);
        }
    }
}

PreenchendoTBVencimentos();*/

